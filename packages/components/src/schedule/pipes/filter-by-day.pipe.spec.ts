import { FilterByDayPipe } from './filter-by-day.pipe';

describe('FilterByDayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByDayPipe();
    expect(pipe).toBeTruthy();
  });
});
